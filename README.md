# ADFIX (Ponthis)

A React admin dashboard template ("ADFIX") built with MUI components, CKEditor 5, and FullCalendar.

Includes theme context, logo management, a rich editor, calendar views, and a polished admin shell (`src/context`, `src/css`, plus bundled image assets).

## Run

```bash
npm install
npm start
```